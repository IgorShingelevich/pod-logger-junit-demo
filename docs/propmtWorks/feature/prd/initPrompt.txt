Нужно написать кастомны екстаншен для junit который будет ставиться каканнотация над тестовым классом
И в тестовом классе выполнять роль сбора информации с лога опе шифт к8 клиента. 
То есть нужно написать кастомный экстеншен для junit который имплементирует тестватчер junita и клиент  опаеншифта
этот экстеншен для укадого теста  фикстирует время начала теста в секции before each и время окончания теста в сексии aftereach.
если в ходе теста произошла ошибка или тест упал по каким то причинам - то в конце теста прикладывается  выборка логов по времени  события в поде сервиса в к8 в Local dateTime из
методом List<PodLogDto> log = openshiftClient.getLog(); 
полученных логов
итак над классом ставится - @PodLogger(collectOnFailOnly=true)
public TestClass{
в сервисе PodLoggerService - автоварим опеншифт клиент и имплементируем TestWatcher -  получаем  в понце теста по апи kuu с поды. из лога получаем только события от времени начала теста до времени конца теста 
прикладывакем аттачем  в AfterEach к кадлому упавшеу тесту  лгги поды сервера за это время к отчету аллюр Allure.addAttavchment.
Реализуй рабочий пример 
@PodLogger(collectOnFailOnly=true)
с возможность. указать - всегда ли прикладывать логи в аннотации непосредственно нвад тестовым классом, или только в случае упашего теста как в примерке.
мне нужен Рабочий проеук на java17, openShiftClient , Junit5, lombok, jackson, пример тестового классаа в клотором запускается параметризованный тест и на каждый парасметр параметризованного теста я получаю логи с поды так как каждый  кейс из датапровыайдера  этого параметризованного теста будет ожидаемо ронять тест
в датапровайдере 3-4 кейса которые должны упасть
Подробности реализации экстеншена  
@PodLogger(collectOnFailOnly=true)
нуэна возможность параметри зации как в примере 
напиши 
@PodLoggerService который будет обслуживать логику екстеншена 
@PodLogger(collectOnFailOnly=true)

@PodLoggerService получает логи по апи с поды afterEach . Выбирает от времени начала теста и прикладывет к аттачу отчета аллюр в afterEach

далее в тестовом классе идет тестовый метод - @ParametrisedTest. так как над тестовым классом стоит такая аннотация то для каждого тестового метода в этомс тестовом классе должна выполнятся логика 
методом List<PodLogDto> log = openshiftClient.getLog(); 

Верни рабочий проект в котором будет реализовано Рабочее решение. Проект должен показать нва примере для демо проекта как работает кастомный экстеншен сбора логов  на джеюните. 
Проект должен соответствовать критериям приемки
А именно Мы должны использоватть ресурсы чтобы  в докере поднять поду какого нибудь простейшего демо приложения, Выполнить к нему обращение по апи Restasured в теле параметризованного теста. Получить ошибку ождаемую. 
для каждого кейса пкараметризованного теста убедиться что ожидамемая ошибка апи собобщения которую я зарегистрирую в crude ответе по апи в тестовом классе с аннотацией - 
стриггерит в аллюр отчете по каждому кейсу появление аллюр аттача с  выборкой логов аот времени начала каждого кейса праметризованного тестаю
То есть не от времени начала всего парам теста а по кадому кейсу из датапрвайдера - время фиксайии ошибко с логов поды должно быть свое.

Верни рабочий проект. Который будет собираться компилироваться в котлором будет тестовый класс с экстеншеном парам тест, который дерагает по ресташуреду ручку с ожидаемой ошибклй.
в проекте дженкинс докер файл собирает нужный стек для демо тестового окружегния. 
Тог есть каое то демо простое приложение с одной двумя ручками и поключенный к этому приложению опеншифт клиент который собирает по апи логи с поды и возвразает в тестовый фреймворк.
OpenshiftClient

Проект должен работать Окружение должно локально собираться в докере. Стартовать. И при запуске тестовго метода обмениваться апи сообщениями с ресташуред клиентом тестового фреймворка. и откидываеть ошибку  в логи поды после ошибки на апи.
Нужен пример из интернета имменногтакого решения rult уже могжно просто забрать в докере готовое приложение и настроенный кубернетис к которому  обращается OpenshiftClient  в тестовом фреймворке за получением лога с ожидаемым Error в логе поды,  после вызова на  апи из тестового класса.

Задай вопросы? ты все понял. Критерии приемки - непротиворечивость иполнота. Не ври. Верни рабочий проект
еще раз    с  докере должно быть бегущее демо приложение публичное. К нему подключен кубернетис на стороне фрймворка тестирования автоварится OpenShiftClientr и методом getLog; получаем лист логов То есть по апи получает  большой лог файл в одной Стринге парсим его джексоном на PodLogDto и выбираем события от времени начала теста. Ты все понял? В проекте  должен бытиь рабочий тест который триггерит ожидаемую ошибку на апи 
ошибка на апи тригшгерит ощибку в лог поды сервиса кбернетиса
 полуая ошибку на апи в тестовом фреймворек - я запрашиваю лгги поды кубернетиса получаю список событиц - и жду  что в логе поды тоже будет ожидаемая зкапись об ошибка вызванная моим тетсом  
Рабочит тест Рабочий тест  Ты понимаешь. Задай вопросы  что тебе не понятно?